#include <bits/stdc++.h>
using namespace std;
void lcm(int a, int b)
{
    // int max = a > b ? a : b;
    // while (true)
    // {
    //     if (max % a == 0 && max % b == 0)
    //     {
    //         cout << "LCM is = " << max << endl;
    //         break;
    //     }
    //     else
    //     {
    //         max += 1;
    //     }
    // }
    // int min = b > a ? a : b;
    // while (min >= 1)
    // {
    //     if (b % min == 0 && a % min == 0)
    //     {
    //         cout << "HCF= " << min;
    //         break;
    //     }
    //     else
    //     {
    //         min -= 1;
    //     }
    // }
}

int main()
{
    int x, y;
    cin >> x >> y;
    lcm(x, y);

    return 0;
}
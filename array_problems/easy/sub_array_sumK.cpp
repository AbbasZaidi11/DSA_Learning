#include <bits/stdc++.h>
using namespace std;
int main()
{
    int arr[] = {10, 5, 2, 7, 1, 9};
    int k = 15;
    vector<int> vec;
    for (auto it : arr)
    {
        vec.push_back(it);
    }
    sort(vec.begin(), vec.end());
    int sum;

    return 0;
}